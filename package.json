{
  "name": "shottr",
  "version": "1.0.0",
  "private": true,
  "description": "MrDemonWolf, Inc. — R2 Image CDN Worker for img.mrdemonwolf.com",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/deploy.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir=dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "@types/aws4": "^1.11.6",
    "@types/node": "^25.6.0",
    "aws4": "^1.13.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5",
    "wrangler": "^4.118.0"
  }
}
